apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

resources:
  - inventory-service-monitor.yaml

patches:
  - path: networkpolicy-patches/inventory-service-ingress-from-monitoring.yaml
    target:
      kind: NetworkPolicy
      name: inventory-service
