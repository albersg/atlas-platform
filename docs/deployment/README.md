# Deployment Docs

- `k3s/RUNBOOK.md`: local non-production deployment runbook for `dev` and `staging` on k3s.
- `gitops/ARGOCD_SOPS_RUNBOOK.md`: Argo CD + KSOPS + SOPS bootstrap and operations.
- `releases/IMAGE_PROMOTION.md`: immutable image publication and staging promotion by digest.
