# k3s Scripts

- `cluster/`: cluster checks, status, and access helpers.
- `images/`: local image build/import helpers for k3s.
- `deploy/`: deployment orchestration flows.

Entry points are exposed through `mise run k8s-*` tasks.
